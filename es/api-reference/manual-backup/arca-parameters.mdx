---
title: 'GET /arca/{name} - Parámetros ARCA'
description: 'Documentación para obtener parámetros de AFIP/ARCA'
---

# 🏛️ GET /arca/{name} - Parámetros ARCA

Este endpoint te permite obtener parámetros de AFIP (Administración Federal de Ingresos Públicos) que están cacheados y gestionados por el sistema. Estos parámetros son esenciales para la facturación electrónica en Argentina.

## URL Base

```
https://facture.ar/api/arca/{name}
```

## Autenticación

Este endpoint requiere autenticación web. Usa el middleware `withWebOnly`.

## Parámetros de Ruta

| Parámetro | Tipo   | Requerido | Descripción                         | Ejemplo                |
| --------- | ------ | --------- | ----------------------------------- | ---------------------- |
| `name`    | string | ✅        | Nombre del parámetro AFIP a obtener | `"FEPARAMGETTIPOSDOC"` |

## Parámetros de Consulta

| Parámetro | Tipo   | Requerido | Descripción                                          | Ejemplo |
| --------- | ------ | --------- | ---------------------------------------------------- | ------- |
| `codId`   | string | ❌        | ID de moneda (requerido para `FEPARAMGETCOTIZACION`) | `"USD"` |

## Parámetros Disponibles

| Parámetro                        | Descripción                                |
| -------------------------------- | ------------------------------------------ |
| `FEPARAMGETCONDICIONIVARECEPTOR` | Tipos de condición de IVA para receptores  |
| `FEPARAMGETTIPOSDOC`             | Tipos de documento                         |
| `FEPARAMGETTIPOSIVA`             | Tipos de IVA                               |
| `FEPARAMGETTIPOSCBTE`            | Tipos de comprobante                       |
| `FEPARAMGETTIPOSMONEDAS`         | Tipos de monedas                           |
| `FEPARAMGETTIPOSCONCEPTO`        | Tipos de concepto                          |
| `FEPARAMGETCOTIZACION`           | Cotizaciones de monedas (requiere `codId`) |

## Respuesta

### Éxito (200)

```json
{
  "ResultGet": {
    "DocTipo": [
      {
        "Id": 80,
        "Desc": "CUIT",
        "FchDesde": "20100101",
        "FchHasta": "NULL"
      },
      {
        "Id": 86,
        "Desc": "CUIL",
        "FchDesde": "20100101",
        "FchHasta": "NULL"
      },
      {
        "Id": 99,
        "Desc": "Consumidor Final",
        "FchDesde": "20100101",
        "FchHasta": "NULL"
      }
    ]
  },
  "Errors": null,
  "Events": null
}
```

### Respuestas de Error

#### Parámetro No Encontrado (404)

```json
{
  "error": "Parameter not found",
  "details": "The requested AFIP parameter does not exist"
}
```

#### Parámetro Requerido Faltante (400)

```json
{
  "error": "Missing required parameter",
  "details": "codId parameter is required for FEPARAMGETCOTIZACION"
}
```

#### Error del Servidor (500)

```json
{
  "error": "Internal server error",
  "details": "Failed to retrieve AFIP parameters"
}
```

## Estructura de Respuesta

| Campo       | Tipo   | Descripción                              |
| ----------- | ------ | ---------------------------------------- |
| `ResultGet` | object | Datos del parámetro solicitado           |
| `Errors`    | array  | Errores de AFIP (null si no hay errores) |
| `Events`    | array  | Eventos de AFIP (null si no hay eventos) |

## Ejemplos de Uso

### Obtener Tipos de Documento

```bash
curl -X GET https://facture.ar/api/arca/FEPARAMGETTIPOSDOC
```

### Obtener Tipos de IVA

```bash
curl -X GET https://facture.ar/api/arca/FEPARAMGETTIPOSIVA
```

### Obtener Cotización de Dólar

```bash
curl -X GET "https://facture.ar/api/arca/FEPARAMGETCOTIZACION?codId=USD"
```

### Obtener Tipos de Comprobante

```bash
curl -X GET https://facture.ar/api/arca/FEPARAMGETTIPOSCBTE
```

### JavaScript/Node.js

```javascript
// Obtener tipos de documento
const response = await fetch('https://facture.ar/api/arca/FEPARAMGETTIPOSDOC', {
  method: 'GET',
  credentials: 'include', // Para autenticación web
});

const result = await response.json();
console.log('Tipos de documento:', result.ResultGet.DocTipo);

// Obtener cotización del dólar
const cotizacionResponse = await fetch('https://facture.ar/api/arca/FEPARAMGETCOTIZACION?codId=USD', {
  method: 'GET',
  credentials: 'include',
});

const cotizacion = await cotizacionResponse.json();
console.log('Cotización USD:', cotizacion.ResultGet);
```

### Python

```python
import requests

# Obtener tipos de documento
response = requests.get(
    'https://facture.ar/api/arca/FEPARAMGETTIPOSDOC',
    cookies={'session': 'your-session-cookie'}  # Para autenticación web
)

result = response.json()
print('Tipos de documento:', result['ResultGet']['DocTipo'])

# Obtener cotización del dólar
cotizacion_response = requests.get(
    'https://facture.ar/api/arca/FEPARAMGETCOTIZACION',
    params={'codId': 'USD'},
    cookies={'session': 'your-session-cookie'}
)

cotizacion = cotizacion_response.json()
print('Cotización USD:', cotizacion['ResultGet'])
```

## Tipos de Documento Comunes

| ID  | Descripción      |
| --- | ---------------- |
| 80  | CUIT             |
| 86  | CUIL             |
| 99  | Consumidor Final |

## Tipos de IVA Comunes

| ID  | Descripción |
| --- | ----------- |
| 3   | 0%          |
| 4   | 10.5%       |
| 5   | 21%         |
| 6   | 27%         |

## Tipos de Comprobante Comunes

| ID  | Descripción |
| --- | ----------- |
| 1   | Factura A   |
| 6   | Factura B   |
| 11  | Factura C   |

## ❓ Solución de Problemas

<Accordion title="Error: Parámetro no encontrado">
**Causa**: El nombre del parámetro no es válido.

**Solución**:

1. Verifica que el nombre del parámetro sea correcto
2. Consulta la tabla de parámetros disponibles
3. Asegúrate de usar el nombre exacto del parámetro

</Accordion>

<Accordion title="Error: Parámetro requerido faltante">
**Causa**: Falta el parámetro `codId` para cotizaciones.

**Solución**:

1. Incluye el parámetro `codId` en la consulta
2. Usa un código de moneda válido (ej: USD, EUR)
3. Verifica que la moneda esté disponible en AFIP

</Accordion>

<Accordion title="Error: No autorizado">
**Causa**: No tienes autenticación web válida.

**Solución**:

1. Asegúrate de estar logueado en la plataforma
2. Incluye las cookies de sesión en la petición
3. Verifica que tu sesión no haya expirado

</Accordion>

## 📞 Soporte

Para soporte técnico o preguntas sobre la API:

- 📧 **Email**: support@facture.ar
- 💬 **Chat**: Disponible en la plataforma
- 📚 **Documentación**: Explora nuestros ejemplos detallados
- 🎥 **Videollamada**: Para asistencia personalizada
